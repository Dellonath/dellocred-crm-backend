export enum CampaignChannel {
  CPC = 'cpc',
  PAID_SOCIAL = 'paid_social',
  EMAIL = 'email',
  SOCIAL = 'social',
  REFERRAL = 'referral',
  PUSH = 'push',
  DISPLAY = 'display',
  INFLUENCER = 'influencer',
  OTHER = 'other',
}

export enum CampaignStatus {
  DRAFT = 'draft',
  SCHEDULED = 'scheduled',
  RUNNING = 'running',
  PAUSED = 'paused',
  COMPLETED = 'completed',
}
